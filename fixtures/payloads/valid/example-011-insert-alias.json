{
  "engine": "sqlite-host-v1",
  "scriptId": "example-011-insert-alias",
  "requiredApiLevel": 1,
  "requiredMethods": [
    "getValue"
  ],
  "steps": [
    {
      "id": "read-current",
      "statements": [
        {
          "sql": "INSERT INTO call_get_value AS c (call_id, input_key) VALUES (:callId, 'example-key')",
          "bindings": {
            "callId": {
              "type": "text",
              "value": "read-1"
            }
          }
        }
      ]
    }
  ]
}
