{
  "engine": "sqlite-host-v1",
  "scriptId": "invalid-result-read-not-after-call",
  "requiredApiLevel": 1,
  "requiredMethods": [
    "getValue",
    "setValue"
  ],
  "steps": [
    {
      "id": "read-and-write",
      "statements": [
        {
          "sql": "INSERT INTO call_get_value (call_id, input_key) VALUES (:callId, 'example-key')",
          "bindings": {
            "callId": {
              "type": "text",
              "value": "read-1"
            }
          }
        },
        {
          "sql": "INSERT INTO call_set_value (call_id, input_key, input_value) SELECT :callId, 'example-key', 42 WHERE EXISTS (SELECT 1 FROM result_get_value WHERE call_id = :readCallId AND status = 'done')",
          "bindings": {
            "callId": {
              "type": "text",
              "value": "write-1"
            },
            "readCallId": {
              "type": "text",
              "value": "read-1"
            }
          }
        }
      ]
    }
  ]
}
