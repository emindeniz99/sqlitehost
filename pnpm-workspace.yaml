# The pnpm workspace is the repository root. Run every pnpm command from
# there; `unity/com.sqlitehost.runtime/package.json` is a UPM manifest and
# is deliberately not a member.

packages:
  - "typespec/library"
  - "codegen/*"
  - "typescript/*"
