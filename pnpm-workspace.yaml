# Self-contained pnpm workspace for the sqlitehost project — see
# CONVENTIONS.md "every project is self-contained". Run pnpm from
# inside projects/sqlitehost/.

packages:
  - "typespec/library"
  - "codegen/*"
  - "typescript/*"
