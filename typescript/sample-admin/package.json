{
  "name": "@sqlite-host/sample-admin",
  "version": "0.2.0",
  "private": true,
  "license": "MIT",
  "description": "Demo admin CLI for SqliteHost: prints the sample host method/table reference and lints a script payload",
  "type": "module",
  "bin": {
    "sqlite-host-demo": "bin/demo.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:web": "node scripts/build-web.mjs",
    "test": "npm run build && npm run build:web && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@sqlite-host/authoring": "workspace:*",
    "@sqlite-host/runtime-types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.5.0"
  }
}
